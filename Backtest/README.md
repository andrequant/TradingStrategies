This is a minimal library to perform backtests.

It contains an class Strategy, a model how strategies can be created.
Also contain the class Backtest, which receive the data and strategy and test it.
